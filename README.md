# Lorett Ground Link Monitor

Система мониторинга станций приёма спутниковых данных с автоматической отправкой ежедневных отчётов на email.

## Возможности

- Скачивание логов с серверов EUS (oper/reg)
- Анализ SNR (Signal-to-Noise Ratio) для каждой станции
- Определение успешных и пустых пролётов
- Генерация графиков пролётов с максимальной суммой SNR
- Построение сводного графика за 7 дней
- Автоматическая отправка статистики на email
- Планировщик для ежедневной отправки в 00:00 UTC

## Требования

- Python 3.8+
- Зависимости (установка: `pip install -r requirements.txt`):
  - requests
  - beautifulsoup4
  - colorama
  - aiohttp
  - matplotlib
  - pyppeteer (для скриншотов графиков)

## Конфигурация

Настройки хранятся в файле `config.json`:

### Станции
```json
{
  "stations": [
    {
      "name": "R2.0S_Moscow",
      "type": "oper",
      "bend": "X"
    },
    {
      "name": "PlanumMoscow",
      "type": "reg",
      "bend": "L"
    }
  ]
}
```

- `name` — имя станции
- `type` — тип сервера (`oper` или `reg`)
- `bend` — диапазон частот (`X` или `L`)

### Email настройки
```json
{
  "email": {
    "enabled": true,
    "smtp_server": "smtp.yandex.ru",
    "smtp_port": 465,
    "sender_email": "your-email@yandex.ru",
    "sender_password": "your-app-password",
    "recipient_email": "recipient@example.com",
    "cc": ["copy1@example.com", "copy2@example.com"]
  }
}
```

- `enabled` — включить/выключить отправку email
- `smtp_server` — SMTP сервер
- `smtp_port` — порт (465 для SSL, 587 для STARTTLS)
- `sender_email` — email отправителя
- `sender_password` — пароль приложения
- `recipient_email` — основной получатель
- `cc` — список адресов для копии (массив или строка через запятую)

### Пороговые значения
```json
{
  "constants": {
    "min_avg_snr_threshold": 4.0,
    "x_bend_failure_threshold": 7.0,
    "l_bend_failure_threshold": 0.0
  }
}
```

## Использование

### Ручной запуск для конкретной даты
```bash
python3 LorettGroundLinkMonitor.py 20260107
```

### Ручной запуск для диапазона дат
```bash
python3 LorettGroundLinkMonitor.py 20260101 20260107
```

### Запуск для текущей даты (UTC)
```bash
python3 LorettGroundLinkMonitor.py
```

### Запуск планировщика (отправка каждый день в 00:00 UTC)
```bash
python3 LorettGroundLinkMonitor.py --scheduler
```

## Автоматический запуск через systemd

### 1. Скопируйте service файл (уже создан)
```bash
# Файл уже создан: /etc/systemd/system/lorett-monitor.service
```

### 2. Перезагрузите systemd
```bash
sudo systemctl daemon-reload
```

### 3. Включите автозапуск при загрузке системы
```bash
sudo systemctl enable lorett-monitor.service
```

### 4. Запустите сервис
```bash
sudo systemctl start lorett-monitor.service
```

### 5. Проверьте статус
```bash
sudo systemctl status lorett-monitor.service
```

### 6. Просмотр логов
```bash
# Последние логи
journalctl -u lorett-monitor.service -n 50

# Логи в реальном времени
journalctl -u lorett-monitor.service -f
```

### Быстрые команды управления
```bash
# Проверить статус
sudo systemctl status lorett-monitor.service

# Посмотреть логи
journalctl -u lorett-monitor.service -f

# Перезапустить
sudo systemctl restart lorett-monitor.service

# Остановить
sudo systemctl stop lorett-monitor.service

# Отключить автозапуск
sudo systemctl disable lorett-monitor.service
```

## Структура проекта

```
LorettGroundLinkMonitor/
├── LorettGroundLinkMonitor.py  # Основной скрипт
├── Logger.py                    # Модуль логирования
├── config.json                  # Конфигурация
├── README.md                    # Документация
├── logs/                        # Скачанные логи
│   └── YYYY/MM/YYYYMMDD/
│       └── StationName/
│           └── *.log
├── report/                      # Графики и отчёты
│   └── YYYY/MM/DD.MM.YYYY/
│       ├── *.png               # Графики пролётов
│       └── overall_unsuccessful_7d.png
└── lorett_monitor.log          # Лог работы скрипта
```

## Формат email отчёта

Письмо содержит:
1. **Таблица сводки** — статистика по каждой станции:
   - Всего пролётов
   - Успешных пролётов
   - Пустых пролётов
   - Процент пустых

2. **Цветовая индикация**:
   - 🟢 Зелёный — менее 2% пустых
   - 🟡 Жёлтый — 2-20% пустых
   - 🔴 Красный — более 20% пустых или нет данных

3. **Графики**:
   - Сводный график за 7 дней
   - Графики пролётов с максимальным SNR для каждой станции

4. **Списки пустых пролётов** — для каждой станции

## Адаптивная вёрстка

Email автоматически адаптируется под размер экрана:
- На широких экранах — полная таблица с 5 колонками
- На узких экранах — горизонтальная прокрутка таблицы

## Логирование

Логи пишутся в:
- `lorett_monitor.log` — основной лог работы скрипта
- `journalctl` — при запуске через systemd

## Устранение неполадок

### Email не отправляется
1. Проверьте настройки в `config.json`
2. Для Яндекс.Почты используйте пароль приложения (не основной пароль)
3. Проверьте, что SMTP порт не заблокирован файрволом

### Графики не генерируются
1. Установите pyppeteer: `pip install pyppeteer`
2. При первом запуске скачается Chromium (может занять время)

### Станция показывает 0 пролётов
1. Проверьте имя станции в `config.json`
2. Проверьте тип сервера (`oper` или `reg`)
3. Проверьте доступность сервера EUS

## Лицензия

MIT License
